name: 자동 포스팅 (Auto Publish to Discussions)

on:
  push:
    branches:
      - main
    paths:
      - '**.md'

permissions:
  contents: write
  discussions: write

jobs:
  publish-discussion:
    runs-on: ubuntu-latest
    steps:
      - name: 코드 가져오기 (Checkout Code)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: false

      - name: 새로 추가된 파일 찾기 (Find Added Files)
        id: changed-files
        uses: tj-actions/changed-files@v45
        with:
          files: '**.md'
          json: true  # JSON 형식 출력

      - name: Discussions에 글 발행하기 (Publish Discussion Post)
        uses: actions/github-script@v7
        env: 
          # 👇👇👇 중요: 데이터를 환경 변수로 안전하게 넘김 (이스케이프 문제 해결)
          ADDED_FILES: ${{ steps.changed-files.outputs.added_files }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            // 1. 환경 변수에서 파일 목록을 가져와 JSON으로 파싱
            const rawFiles = process.env.ADDED_FILES;
            
            if (!rawFiles) {
              console.log('새로 추가된 파일이 없습니다 (No new files).');
              return;
            }

            let files = [];
            try {
              files = JSON.parse(rawFiles);
            } catch (e) {
              console.log('JSON 파싱 에러 (무시됨):', e.message);
              return;
            }

            if (files.length === 0) {
              console.log('파일 목록이 비어 있습니다.');
              return;
            }

            const fs = require('fs');

            // 2. 리포지토리 및 카테고리 ID 조회
            const query = `query($owner:String!, $name:String!) { repository(owner:$owner, name:$name) { id discussionCategories(first:10) { nodes { id name } } } }`;
            const variables = { owner: context.repo.owner, name: context.repo.repo };
            const result = await github.graphql(query, variables);
            const repoId = result.repository.id;
            const category = result.repository.discussionCategories.nodes.find(n => n.name === 'General');
            const categoryId = category ? category.id : result.repository.discussionCategories.nodes[0].id;

            // 3. 글 발행 로직
            for (const file of files) {
              if (!file || file.trim() === '') continue;
              
              const title = file.split('/').pop().replace('.md', '');
              let body = '';
              try {
                body = fs.readFileSync(file, 'utf8');
              } catch (e) {
                console.log(`❌ 파일 읽기 실패: ${file}`);
                continue;
              }
              
              const mutation = `mutation($repositoryId:ID!, $categoryId:ID!, $title:String!, $body:String!) { createDiscussion(input: {repositoryId: $repositoryId, categoryId: $categoryId, title: $title, body: $body}) { discussion { url } } }`;
              
              try {
                const response = await github.graphql(mutation, { repositoryId: repoId, categoryId: categoryId, title: title, body: body });
                console.log(`✅ 발행 완료: ${title} - ${response.createDiscussion.discussion.url}`);
              } catch (error) {
                console.log(`❌ 발행 실패: ${error.message}`);
              }
            }
